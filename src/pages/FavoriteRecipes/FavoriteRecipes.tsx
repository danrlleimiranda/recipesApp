function FavoriteRecipes() {
  return (
    <div
      data-testid="page-title"
      style={ {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
      } }
    >
      FavoriteRecipes

    </div>

  );
}
export default FavoriteRecipes;
